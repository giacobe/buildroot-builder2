# basic

Minimal PolyLinux/v86 Buildroot baseline for shell setup and simple labs.

Includes BusyBox, Bash, file inspection, find/grep/awk/sed/coreutils, and
shadow user-management tools. Outputs `bzImage` and `rootfs.cpio.gz`.
