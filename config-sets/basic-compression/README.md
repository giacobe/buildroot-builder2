# basic-compression

PolyLinux/v86 baseline for labs that need archive and compression tools.

Includes the `basic` runtime plus binutils, Vim, gzip, bzip2, xz, tar, zip,
unzip, lz4, zstd, brotli, lzop, lrzip, p7zip, and pigz.
